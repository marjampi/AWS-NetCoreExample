# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: dotnet build AWS-NetCoreExample/src/AWS-NetCoreExample
    - name: Test
      run: dotnet test AWS-NetCoreExample/test/AWS-NetCoreExample.Tests
    - name: Install Amazon.Lambda.Tools
      run: dotnet tool install -g Amazon.Lambda.Tools
    - name: Deploy
      run: |
        cd AWS-NetCoreExample/src/AWS-NetCoreExample
        dotnet lambda package
        dotnet lambda deploy-function HelloTestEnv --region eu-north-1 -frun dotnet6 --aws-access-key-id AKIAXNAJTJEESMJSF4U7 --aws-secret-key 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 --disable-interactive true

